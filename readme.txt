to launch server locally, make sure you have node.js installed.
in the ProjectChefShop directory run "npm install"
after that run "npm run dev" for the server to update everytime there is a change
or "run npm start" for a normal server

commands to start server
npm install
npm run dev, or, 
npm run start